<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
</head>
<body>
    <% if(headerEnabled) { include('./shared/header.ejs'); } %>
    <main>
        <%- include(`./${page}/${page}.ejs`); %>
    </main>
    <footer>
    </footer>
</body>
</html>